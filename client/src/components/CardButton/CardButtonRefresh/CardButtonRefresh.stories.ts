import { Meta, StoryObj } from "@storybook/react";
import CardButtonRefresh from "./CardButtonRefresh";

export default {
    component: CardButtonRefresh,
    title: "Button/CardButton",
} as Meta<typeof CardButtonRefresh>;

export const Refresh: StoryObj<typeof CardButtonRefresh> = {};
