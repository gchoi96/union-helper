export enum BLOCK_STATUS {
    IN_USE,
    NOT_IN_USE,
}

export enum CELL_STATUS {
    OCCUPIED,
    TO_BE_OCCUPIED,
    AVAILABLE,
    UNAVAILABLE,
}

export enum BUTTON_COLOR {
    RED,
    BLUE,
    GREEN,
}

export enum CHECK_STATUS {
    CHECKED,
    UNCHECKED,
}
