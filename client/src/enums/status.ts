export enum BLOCK_STATUS {
    IN_USE,
    NOT_IN_USE,
}

export enum CELL_STATUS {
    SELECTED,
    NOT_SELECTED,
    PLACED
}

export enum BUTTON_TYPE {
    RED,
    BLUE,
    GREEN,
}

export enum CHECK_STATUS {
    CHECKED,
    UNCHECKED,
}
