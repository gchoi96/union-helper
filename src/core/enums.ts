export enum BLOCK_STATUS {
    IN_USE,
    NOT_IN_USE,
}

export enum CELL_STATUS {
    OCCUPIED,
    TO_BE_OCCUPIED,
    AVAILABLE,
    UNAVAILABLE,
}

export enum CHECK_STATE {
    CHECKED,
    UNCHECKED,
}
